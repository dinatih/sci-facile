<nav class="navbar navbar-expand-lg navbar-light bg-light fixed-top shadow">
  <div class="container">
    <span class="navbar-brand fw-bold text-primary">
      <%= link_to root_path, class: "text-decoration-none" do %>
        SCI-Facile <i class="fas fa-building"></i>
      <% end %>
      <%= link_to @company.name, company_path(@company), class: "text-decoration-none" %>
    </span>
    <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav" 
            aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse" id="navbarNav">
      <ul class="navbar-nav ms-auto">
        <li class="nav-item">
          <%= link_to company_associates_path(@company), class: "nav-link" do %>
            <i class="fas fa-user-group text-primary"></i>
            <%= t("activerecord.models.associate.other") %>
          <% end %>
        </li>
        <li class="nav-item">
          <%= link_to company_properties_path(@company), class: "nav-link" do %>
            <i class="fas fa-building text-primary"></i>
            <%= t("activerecord.models.property.other") %>
          <% end %>
        </li>
        <li class="nav-item">
          <%= link_to company_financial_operations_path(@company), class: "nav-link" do %>
            <i class="fas fa-file-invoice-dollar text-primary"></i>
            <%= t("activerecord.models.financial_operation.other") %>
          <% end %>
        </li>
        <li class="nav-item">
          <%= link_to company_tenants_path(@company), class: "nav-link" do %>
            <i class="fas fa-home-user text-primary"></i>
            <%= t("activerecord.models.tenant.other") %>
          <% end %>
        </li>
        <li class="nav-item">
          <%= link_to company_general_meetings_path(@company), class: "nav-link" do %>
            <i class="fas fa-pen text-primary"></i>
            <%= t("activerecord.models.general_meeting.other") %>
          <% end %>
        </li>
        <% if associate_signed_in? %>
          <li class="nav-item ms-2">
            <%= link_to current_associate.email, company_associate_path(current_associate.company, current_associate), class: "nav-link" %>
          </li>
          <li class="nav-item">
            <%= link_to destroy_associate_session_path, data: { turbo_method: "delete" }, class: "nav-link", title: "Déconnexion" do %>
              <i class="fas fa-sign-out-alt"></i>
            <% end %>
          </li>
        <% else %>
          <li class="nav-item ms-2">
            <%= link_to "Connexion", new_session_path(:associate), class: "btn btn-outline-primary rounded-pill" %>
          </li>
        <% end %>
      </ul>
    </div>
  </div>
</nav>