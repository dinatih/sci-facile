<% content_for :title, t("helpers.new", model: t("activerecord.models.general_meeting.one")) %>

<h1 class="h4 mb-3"><i class="fas fa-pen text-primary me-2"></i><%= t("helpers.new", model: t("activerecord.models.general_meeting.one")) %></h1>
<div class="card">
  <div class="card-body">
    <%= render "form", general_meeting: @general_meeting %>
  </div>
</div>

<div class="mt-3">
  <%= link_to t("helpers.back", model: t("activerecord.models.general_meeting.other")), company_general_meetings_path(@company), class: "btn btn-link" %>
</div>
