<% content_for :title, t("helpers.edit", model: t("activerecord.models.general_meeting.one")) %>

<div class="container my-4">
  <h4 class="mb-3">
    <i class="fas fa-pen text-primary me-2"></i>
    <%= t("helpers.edit", model: t("activerecord.models.general_meeting.one")) %>
  </h4>

  <%= render "form", company: @company, general_meeting: @general_meeting %>

  <div class="mt-3 d-flex gap-2">
    <%= link_to t("helpers.back", model: t("activerecord.models.general_meeting.other")), company_general_meetings_path(@company), class: "btn btn-link" %>
  </div>
</div>