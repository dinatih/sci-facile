<% content_for :title, t("activerecord.models.general_meeting.one") %>

<%= render @general_meeting %>

<div>
  <%= link_to t("helpers.edit", model: t("activerecord.models.general_meeting.one")), edit_company_general_meeting_path(@company, @general_meeting), class: "btn btn-outline-primary" %> |   
  <%= link_to t("helpers.back", model: t("activerecord.models.general_meeting.other")), company_general_meetings_path(@company), class: "btn btn-outline-primary" %>

  <%= button_to t("helpers.destroy", model: t("activerecord.models.general_meeting.one")), [@company, @general_meeting], method: :delete, class: "btn btn-danger mt-2" %>
</div>