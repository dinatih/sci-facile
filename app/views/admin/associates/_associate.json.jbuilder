json.extract! associate, :id, :company_id, :first_name, :last_name, :email, :shares_count, :initial_contribution, :current_account_balance, :created_at, :updated_at
json.url associate_url(associate, format: :json)
