<% content_for :title, t("activerecord.models.company.one") %>

<%= render @company %>
<%= link_to t("helpers.edit", model: t("activerecord.models.company.one")), edit_company_path(@company), class: "btn btn-outline-primary" %>   
