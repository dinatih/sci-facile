<% content_for :title, t("activerecord.models.tenant.other") %>

<div class="container my-4">
  <div class="d-flex justify-content-between align-items-center mb-3">
    <h4 class="mb-0">
      <i class="fas fa-house-user text-primary me-2"></i>
      <%= t("activerecord.models.tenant.other") %>
    </h4>
    <%= link_to t("helpers.new", model: t("activerecord.models.tenant.one")), new_company_tenant_path(@company), class: "btn btn-primary" %>
  </div>

  <div id="tenants" class="row g-3">
    <% @tenants.each do |tenant| %>
      <div class="col-12 col-md-6 col-lg-4">
        <%= render tenant, show_actions: true %>
      </div>
    <% end %>
  </div>

  <div class="mt-3">
    <%= link_to t("helpers.back", model: t("activerecord.models.company.one")), company_path(@company), class: "btn btn-link" %>
  </div>
</div>